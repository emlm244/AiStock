# Development dependencies are optional.  Pin exact versions if you choose to
# introduce tooling (linters, formatters, docs).  The safe baseline requires
# none, so this file intentionally stays empty.
